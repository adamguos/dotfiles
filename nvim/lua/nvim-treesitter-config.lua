-- config_file_path=~/.config/nvim/lua/nvim-treesitter-config.lua

require('nvim-treesitter.configs').setup({
    highlight = {
        enable = true
    },
    indent = {
        enable = true
    }
})
